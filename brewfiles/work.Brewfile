# ============================================================
# Layer: work
#
# Policy: Add tools via mise when possible; keep Brewfile entries for
# casks, language servers only packaged as brew, host deps mise does not cover,
# and tools that work requires via homebrew that I don't have on my personal machines
#
# Purpose:
# - Tools that are only used on my work machine, probably from a private tap
# - We don't do container-first setup at work in all places, so I will need dbs and whatnot, unfortunately.
#
# ============================================================


cask "aws-vault-binary"
cask "yubico-authenticator"

cask "gusto-wkhtmltopdf"
brew "gusto-lima"