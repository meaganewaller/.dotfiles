#!/usr/bin/env lua

-- Sketchybar configuration using SbarLua
-- https://github.com/FelixKratz/SbarLua
--
-- Features:
-- - Theme-aware colors (reads ~/.config/theme/current.json)
-- - Aerospace workspace integration (configurable via settings.lua)
-- - VPN status indicator
-- - Battery, volume, wifi, cpu widgets
-- - Media controls
-- - Theme change event support (trigger with: sketchybar --trigger theme_changed)

-- Load the sketchybar package and prepare helper binaries
require("helpers")
require("init")
