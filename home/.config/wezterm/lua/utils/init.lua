icons = require("lua.utils.icons")
path = require("lua.utils.path")
tabs = require("lua.utils.tabs")
time = require("lua.utils.time")

local M = {
  icons = icons,
  path = path,
  tabs = tabs,
  time = time,
}

return M