{
    // Read permissions for specific files for Common profile
    // NOTE: to add reading directories, use the additional-dirs.jsonc file
    "$schema": "https://json.schemastore.org/claude-code-settings.json",
    "permissions": {
        "allow": [
            "Read(~/.zshrc)"
        ],
        "deny": [
            "Read(~/.ssh/**)",
            "Read(.env)"
        ]
    }
}
