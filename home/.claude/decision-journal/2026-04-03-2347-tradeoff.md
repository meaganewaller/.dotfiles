# Tradeoff: 2026-04-03

**Branch:** main
**Files:** /Users/meaganwaller/github/onlooker-community/website/src/pages/Home.tsx
**Lines Changed:** 153
**Change Type:** unknown
**Source:** auto-capture

## Context

Large change detected with tradeoff discussion signals.

## Raw Context (for review)

```
- `Home.tsx` — Quickstart steps 1-2 now reference `/plugin` and the marketplace instead of `git clone`
- `plugins/Tribunal.tsx` — Install step uses `/plugin` instead of `git clone`, verification checklist updated, prerequisite text references marketplace
- `plugins/Archivist.tsx` — Install step uses `/plugin` instead of `cp -r`, intro text references marketplace
```

## Notes

_Auto-captured. Review and edit as needed._
