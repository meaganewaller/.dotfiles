# Dev-OS norms

- **Tradeoffs:** Document significant decisions in `~/.claude/dev-os-events.jsonl` with `event_type: decision_tradeoff`.
- **Tests:** Fix or run tests before stopping when the last test run failed.
- **Tasks:** Create a task list (Task tool) before large work so context can compact safely.
