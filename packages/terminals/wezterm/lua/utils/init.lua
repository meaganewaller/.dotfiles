local icons = require("lua.utils.icons")
local path = require("lua.utils.path")
local tabs = require("lua.utils.tabs")
local time = require("lua.utils.time")

local M = {
  icons = icons,
  path = path,
  tabs = tabs,
  time = time,
}

return M
